# mac 分支（SenseVoice.cpp）

`main` 不含 `SenseVoice.cpp`。Mac 本地 **cpp / Metal** 后端在 **`mac` 分支**：

```bash
git checkout mac
```

该分支 vendor `SenseVoice.cpp/`，默认 `SENSE_VOICE_BACKEND=cpp`。完整说明见 checkout 后的本文档（mac 分支版本）。
